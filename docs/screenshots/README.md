# Screenshot evidence index

| File | What it proves |
|---|---|
| `login-request.png` | Demo authentication request configured in Swagger. |
| `swagger-authorized.png` | Swagger authenticated with the owner bearer token. |
| `widget-api-response.png` | Owner widget configuration and public ID returned by API. |
| `cross-origin-widget-loaded.png` | Widget bundle rendered on the second-origin customer page. |
| `widget-form-filled.png` | Visitor completed the configured fields. |
| `submission-success.png` | Public submission accepted and visitor confirmation shown. |

Token-bearing or troubleshooting-only screenshots are stored locally in `docs/private-screenshots/`, which is intentionally gitignored. Do not add them to a public repository.
